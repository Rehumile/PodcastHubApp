import { createTheme } from '@mui/material/styles'

const theme = createTheme({
    palette: {
        primary: {
            main: '#003EAB',
        },
        secondary: {
            main: '#008033'
        },
        background: {
            default: '#EEF3F6'
        }
    },
}) 

export default theme